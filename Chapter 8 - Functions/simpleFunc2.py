#Passing information into a function

def greet_user(userName):
    print(f"Hello {userName} that is a beautiful name! ")

greet_user("Eve")

#Arguments and Parameters the parameter in the greet_user function is userName
#An argument is the data being passed within the function call to the function
#People use these terms interchangably
